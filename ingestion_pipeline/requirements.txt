# Ingestion pipeline dependencies
# These are a subset of the root requirements.txt scoped to the ingestion module only.
# If running the full project via Docker, use the root requirements.txt instead.

requests==2.31.0
pandas==2.2.1
dlt[bigquery]==0.4.5
python-dotenv
